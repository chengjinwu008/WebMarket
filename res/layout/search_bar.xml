<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:background="@color/red"
                android:layout_height="match_parent">
    <ImageView android:layout_alignParentLeft="true" android:id="@id/search_bar_logo" android:src="@drawable/logo"
               android:layout_width="80dp" android:layout_height="match_parent"/>
    <ImageButton android:background="@color/red" android:src="@drawable/search_button" android:id="@id/search_bar_btn"
                 android:layout_width="wrap_content" android:layout_height="match_parent"
                 android:layout_alignParentRight="true"/>
    <EditText
            android:layout_marginTop="5dp"
            android:layout_marginBottom="5dp"
            android:background="@color/white"
            android:layout_width="match_parent" android:layout_height="match_parent" android:hint="@string/search_hint"
            android:layout_toRightOf="@id/search_bar_logo"
            android:layout_toLeftOf="@id/search_bar_btn"
            android:textColorHint="@color/grey"
            android:textColor="@color/black"
            />
</RelativeLayout>